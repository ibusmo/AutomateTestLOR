package testdata;

public class CellTag {
	public enum col{
		A, B, C, D, E, F, G, H, I, J, K
	}
	public enum inputType{
		dropdown, text, radio, date, button, checkbox, popup, alert
	}
	public enum fieldType{
		id, name, xpath, linktext
	}
}
